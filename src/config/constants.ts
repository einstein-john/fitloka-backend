export enum NodeEnvOptions {
  STAGING = "staging",
  PRODUCTION = "production",
  DEVELOPMENT = "development",
}

export enum DebugOptions {
  DEVELOPMENT = "dev",
  STAGING = "stag",
  PRODUCTION = "prod",
}
